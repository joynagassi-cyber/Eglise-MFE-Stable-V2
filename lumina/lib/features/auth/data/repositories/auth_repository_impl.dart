// ⛔ DEAD CODE — Use SupabaseAuthRepository instead (bound in repository_providers_auth.dart).
// All imports have been confirmed absent (grep: zero).
// ponytail: emptied — not deleted to preserve git history; zero imports → no compile impact.
